import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function Home() {
  return (
    <section className=" bg-neutral-700 w-screen h-screen flex items-center justify-center">
      
    </section>
  );
}
